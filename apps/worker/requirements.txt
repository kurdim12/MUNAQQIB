# --- Web framework / server ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Config / models ---
pydantic==2.10.4
pydantic-settings==2.7.1
PyYAML==6.0.2

# --- HTTP / scraping ---
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
# Playwright is optional — install only if a source requires JS (CLAUDE.md §3).
# playwright==1.49.1

# --- Data / DB ---
supabase==2.11.0

# --- Embeddings (offline, Arabic-capable, 384-dim). Heavy; optional at runtime. ---
# The matcher degrades to keyword-only if these are absent (see DECISIONS.md).
sentence-transformers==3.3.1

# --- Analyzer (Phase 2) ---
pypdf==5.1.0
# OCR fallback requires the system `tesseract-ocr` + `tesseract-ocr-ara` packages.
pytesseract==0.3.13
pdf2image==1.17.0

# --- LLM clients ---
anthropic==0.42.0
openai==1.59.6   # used as the OpenRouter-compatible client for cheap-model slots

# --- Dev / test ---
pytest==8.3.4
