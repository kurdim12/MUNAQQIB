# Lightweight dependency set for dev / CI / web sessions.
# Installs everything needed to import the worker, run the test suite, and lint —
# WITHOUT the heavy optional ML stack (sentence-transformers/torch) and Phase-2
# OCR deps. The matcher degrades to keyword-only when embeddings are absent
# (see DECISIONS.md), so the full pipeline still imports and tests pass.
#
# For the real runtime (with embeddings + analyzer), install requirements.txt.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
PyYAML==6.0.2
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0

# Test + lint
pytest==8.3.4
ruff==0.8.4
